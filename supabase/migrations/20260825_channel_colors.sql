-- 유입경로 도넛 차트 채널별 색상 커스터마이즈 — 2026-08-25
--
-- 전부 **추가만** 한다. prisma db push 는 쓰지 않는다(부분 유니크 인덱스가 날아간다). 재실행 안전.

-- ── Workspace: 채널(UTM 소스)별 사용자 지정 색 ────────────────────────
-- legalProfile 과 같은 이유로 워크스페이스 자산이다 — 채널 색은 프로젝트가 아니라
-- 팀이 쓰는 마케팅 채널 전체에 대한 브랜딩이라 워크스페이스 전체가 공유한다.
-- { "네이버": "#0a9e6e", "google": "#2a6fdb", ... } — 정규화된(소문자) 채널 라벨 → hex.
-- null = 아직 커스텀 안 함(코드에 내장된 브랜드 기본 팔레트 + 해시 폴백을 그대로 씀).
ALTER TABLE "Workspace" ADD COLUMN IF NOT EXISTS "channelColors" JSONB;
