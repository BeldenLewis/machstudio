-- 수집 데이터 표에서 특정 필드를 열로 안 보이게 숨기는 토글.
--
-- 왜: 대행전시 소스(에듀테크 등)는 사이트가 갖고 있는 항목 중 우리 폼에서는 안 쓰는 항목
-- (관람 예정 시간, 구분 등)까지 그대로 따라와 표가 지저분해진다. 값 자체는 계속 수집·보관하고
-- (레코드 상세·CSV 내보내기는 그대로 전부 보여줌), "평소에 훑어보는 표"에서만 뺄 수 있게 한다.
--
-- 순수 추가이고 기본값 false 로 시작한다. 기존 매핑은 전부 오늘과 같이 표에 그대로 보인다.
ALTER TABLE "FieldMapping" ADD COLUMN IF NOT EXISTS "hidden" BOOLEAN NOT NULL DEFAULT false;
